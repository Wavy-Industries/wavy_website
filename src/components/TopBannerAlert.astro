
<section id="top-banner">
  <!-- <div id="remove-btn">×</div> -->
  <div class="track">
    <span><slot /></span>
    <span aria-hidden="true"><slot /></span>
  </div>
</section>

<style>
:root {
  --banner-color: rgb(137,112,88);
}
section {
  width: 100%;
  padding: 0px 15px;
  background-color: var(--banner-color);
  color: white;

  font-weight: 400;
  font-size: 14px;
  font-style: italic;

  white-space: nowrap; /* Prevent text wrapping */
  overflow: hidden; /* Hide overflowing content */
  position: relative;

  display: flex;
  align-items: center;
}

@keyframes scroll {
  0% {
    transform: translateX(0);
  }
  100% {
    transform: translateX(-50%); /* Exactly one copy, so the loop is seamless */
  }
}

.track {
  display: flex;
  width: max-content;
  animation: scroll 15s linear infinite;
}

section span {
  flex: 0 0 auto;
  /* At least a full viewport so the copies never bunch up, but wider when the
     message itself is longer than the viewport (narrow phones). */
  min-width: 100vw;
  padding: 5px 0;
}

@media (prefers-reduced-motion: reduce) {
  section {
    white-space: normal;
  }
  .track {
    animation: none;
    width: 100%;
  }
  section span {
    min-width: 0;
    width: 100%;
    text-align: center;
  }
  section span + span {
    display: none;
  }
}

</style>
